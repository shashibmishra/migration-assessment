apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.csv: |
    p, role:observability-admin, applications, get, */*, allow
    p, role:observability-admin, applications, create, */*, allow
    p, role:observability-admin, applications, update, */*, allow
    p, role:observability-admin, applications, sync, */*, allow
  policy.default: role:readonly
  scopes: groups
